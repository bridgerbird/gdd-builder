import React from "react";
import ReactDOM from "react-dom/client";
import GDDBuilder from "./GDDBuilder.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GDDBuilder />
  </React.StrictMode>
);
